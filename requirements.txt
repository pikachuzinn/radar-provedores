# Dependências obrigatórias
requests>=2.31.0        # Chamadas HTTP à Google Places API
python-dotenv>=1.0.0    # Leitura do arquivo .env com a chave de API

# Dependência opcional (necessária apenas para exportação em Excel)
openpyxl>=3.1.0         # Leitura/escrita de .xlsx

# Dependências de desenvolvimento (necessárias apenas para rodar a suíte de testes)
pytest>=8.0.0           # Execução dos testes em tests/

# Dependências obrigatórias
requests>=2.31.0        # Chamadas HTTP à Google Places API
python-dotenv>=1.0.0    # Leitura do arquivo .env com a chave de API

# Dependências opcionais (necessárias apenas para exportação em Excel)
pandas>=2.0.0           # Manipulação de dados tabulares
openpyxl>=3.1.0         # Motor de leitura/escrita .xlsx usado pelo pandas
